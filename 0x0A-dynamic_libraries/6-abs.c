#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an int
 *
 * @n: the int to compute
 *
 * Return: the absolute value of int n
 */

int _abs(int n)
{
	return ((n < 0) ? -n : n);
}
